#!/bin/bash

export LABOPS_HOME="/opt/labops"

if [ -f "$LABOPS_HOME/VERSION" ]; then
    export LABOPS_VERSION="$(cat "$LABOPS_HOME/VERSION")"
else
    export LABOPS_VERSION="v0.0.0 - Unknown"
fi

source "$LABOPS_HOME/core/engine.sh"

labops_boot
